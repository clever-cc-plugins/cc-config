# Learnings

Corrections and observations collected during configuration sessions.
Entries are tagged by skill and dated.

---

[cc-config:cc-config-optimize] This repo is the cc-config plugin's own source — the two SKILL.md files under plugins/cc-config/skills/ (464/491 lines) are the distributed product, not per-message overhead, so the "<50 lines per skill" bloat check doesn't apply to them here — 2026-07-17
